# Defaults for mtail initscript
# sourced by /etc/init.d/mtail
# installed at /etc/default/mtail by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_ARGS="-progs /etc/mtail -log_dir /var/log/mtail -logs /var/log/mail.log"
